### Challenge Name:
Flash authentication

### Goal:
Find the validation code.

### Walkthrough:
- Initial observation:
	- I Opened the challenge page and showed the white page.
    - Observed a Flash file (`.swf`) instead of normal HTML input in source page.
    - See the file type and recognized Flash is compiled, not human-readable
    - Opened the SWF file and saw unreadable binary content
- Decompiler
	- Used googling, to know for .swf extension, use the FFdec to decompile
	- Download the FFdec.
	- After opening the JPEXS free flash decompiler, open the RootMe.swf file.
	- In FFDec: Found embedded binary file (`.bin`) and XOR function with a hardcoded key
	-  Understood XOR logic: `data[i] ^= key[i % len(key)]`
	- Since XOR is reversible, used same logic to decrypt embedded file.
	- I export all parts from the ffdec and navigate to the directory.
	- Decrypted the file by using the key and encrpted file using the xor logic
